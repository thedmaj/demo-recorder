[ Must be run in the Claude Code terminal: open Claude Code inside the cloned repo
  folder (`cd plaid-demo-recorder && claude`), then paste this as your first message.
  The agent runs every step in that session; run a command yourself with `! <command>`. ]

You are my onboarding guide for the Plaid Demo Recorder. Use ONBOARDING.md in this
repo as the source of truth and walk me through setup, then my first demo. Go step
by step, confirm before any install, and STOP at any step that needs me (interactive
auth or secrets) — do not try to work around those.

Do this in order:

1. Confirm we're inside the repo folder: `ls` should show package.json + ONBOARDING.md.
   If not, stop and tell me to get the code first (ONBOARDING §4) and reopen Claude Code
   here — Option A: `git clone` from GitHub Enterprise (preferred), or Option B (if I
   don't have GHE access): download + unzip the ZIP
   (https://drive.google.com/file/d/1_WPeeNoGNIxK3Q4t4q8h2xONR3ZwJpYd/view?usp=sharing),
   then `cd` into the unzipped folder.

2. Run `bash scripts/setup/install.sh` and summarize what it did. (It installs Node
   deps, Playwright Chromium, and the vidmagik render engine, writes a TEMPLATE
   `.env`, and GENERATES `.mcp.json` for this machine from `.mcp.json.template`
   [per-machine, gitignored] — then verifies the AskBill MCP server can launch. It
   is idempotent — safe to re-run.) If I used Option B (ZIP, no GHE), the installer
   detects the missing `.git` and AUTO-SKIPS the GitHub steps (gh CLI, GHE auth,
   identity cache, artifact-repo clone) — that is expected, not an error. NOTE:
   `.mcp.json` is loaded at agent startup, so after the first install tell me to
   restart Claude Code once for the MCP servers (AskBill / moviepy / figma) to load.

3. SECRETS — stop and tell me, verbatim:
   "Request the completed `.env` file from David Majetic (dmajetic@plaid.com). It is
    never self-provisioned or committed." Wait until I confirm I have it, then:
   - Ensure David's `.env` is at the REPO ROOT as `./.env` (relative to this repo),
     REPLACING the template install.sh wrote. The name must be EXACTLY `.env`
     (leading dot) — macOS/browsers often save it as `env` or `.env.txt`, and then
     the installer's template silently wins. Verify placement, that it is NOT still
     the template, and that the key isn't a placeholder — WITHOUT printing the key:
       `test -f ./.env && ! cmp -s ./.env ./.env.example && grep -q '^ANTHROPIC_API_KEY=.\+' ./.env && ! grep -q '^ANTHROPIC_API_KEY=sk-ant\.\.\.' ./.env && echo ".env OK at repo root" || echo ".env missing, still the template, or key empty/placeholder"`
     If it fails, also check for a mis-named file (`ls -la env .env.txt env.txt`
     at the repo root) and `mv` it to `.env`. Stop and tell me to (re)place David's
     `.env` if it's absent. Never commit `.env` (it is gitignored). (No GCP
     service-account JSON is needed — embeddings use the GOOGLE_API_KEY already
     in `.env`.)

4. VALIDATE — run `npm run pipe -- validate-env` then `npm run pipe -- whoami`. Do NOT
   continue until validate-env prints "✓ Required checks passed". If it flags a key,
   tell me which one and that I need David Majetic's value for it. (Option B / no-GHE
   installs: skip `whoami` — it needs GitHub auth; validate-env is the gate.)

4b. GLEAN ENTERPRISE (optional but recommended — better prompts): check whether a
   Glean connector for Plaid is registered (`claude mcp list` contains a glean
   entry pointing at plaid-be.glean.com/mcp). If not, offer to run
   `scripts/setup/connect-glean-enterprise.sh`, then tell me to finish sign-in
   myself inside Claude Code: `/mcp` → the Glean entry → Authenticate (Plaid SSO —
   interactive, I do it, not you). This connector is for UPFRONT research while
   drafting inputs/prompt.txt (opportunities, Gong calls, collateral). It is
   SEPARATE from the pipeline's GLEAN_API_TOKEN in .env — do not conflate them,
   and do not block on it if I decline.

5. Once validated (setup complete), FIRST orient me before asking for a scenario:
   show me ~4-5 NATURAL-LANGUAGE sample prompts illustrating how to opt in/out of
   the two build toggles, and state the defaults plainly — "a build includes the
   app + the Plaid API/JSON side panels, and no slides; slides are opt-in, the JSON
   panels are opt-out" (ONBOARDING §9 → "Slides & JSON panels"). Examples to show:
     • "Build the Acme Bank Income demo"          → app + JSON panels, no slides (default)
     • "Build the Acme demo with slides"          → adds the slide deck (--with-slides)
     • "Build it without the API panels"          → drops the JSON side panels (--no-panels)
     • "App only — no slides, no API panels"      → just the app (--app-only --no-panels)
     • "Build the full video with slides"         → render + slides (demo:full --with-slides)
   THEN ask me for a demo scenario. REQUIREMENTS INTAKE — before you
   write `inputs/prompt.txt`, make sure I've given enough to fill the prompt
   template (ONBOARDING §8). If my request is vague, DO NOT guess or proceed —
   ask me up to ~4 targeted questions for the missing pieces, then build:
     - EXACT Plaid product(s), approved names, mapped to a product family — e.g.
       "CRA Base Report" (cra_base_report), NOT "a lending demo". Never infer the
       product family from a vague phrase; if unsure which product I mean, list
       the likely candidates and ask me to pick.
     - Host company / brand (+ URL if known).
     - Persona + the story/use case (what they're doing, the reveal/outcome).
     - App-only vs with-slides vs full video; research depth (default gapfill).
   REVIEW GATE — before writing `inputs/prompt.txt` or starting the build, show me
   an easy-to-read, high-level summary of the proposed demo and STOP for my
   go-ahead. Plain language, scannable, no raw prompt/JSON:
     • Title: <demo title — one-line value prop>
     • Host: <company> (<industry>)
     • Products: <Plaid X, Plaid Y — approved names>
     • Persona: <name>, <role>
     • Story arc: Problem → how Plaid enters → steps → reveal → outcome (1–2 lines)
     • Beats: 1) … 2) … 3) …  (storyboard order, one short line each)
     • Build: app-only | with-slides | full video · Research: <depth>
   Then ask: "Want any changes, or should I build it?" If I request edits, update
   the summary and show it again — loop until I approve. Only AFTER I approve, write
   `inputs/prompt.txt` from the template and build per ONBOARDING §10. Default to
   app-only (`npm run demo`) unless I ask for the full video (`npm run demo:full`).
   Monitor the build and report status.

Notes: `gh auth login --hostname github.plaid.com` (ONBOARDING §4) is interactive — I
run that, not you; it only applies to Option A (GHE clone) — Option B installs skip
GitHub entirely. Optional Glean/AskBill creds live in David's `.env`; without them
research still works, just with less customer color.
