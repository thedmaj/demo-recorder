================================================================================
Tilt — Plaid Protect Trust Index for cash-advance and LOC fraud screening
================================================================================

**Host:** **Tilt** (Tilt Finance Inc., fka emPower LLC) — consumer fintech: cash advance, credit cards (WebBank), line of credit (FinWise Bank)
**Canonical URL:** https://tilt.com/
**Brand URL** (for `brand-extract`): https://tilt.com/

**Opportunity context (Salesforce — open, Evaluation; Glean salescloud):**
- Opportunity: `Tilt - Protect` (`006UV00000XtaPdYAJ`)
- URL: https://plaidinc.lightning.force.com/lightning/r/Opportunity/006UV00000XtaPdYAJ/view
- Stage: `Evaluation`  •  Forecast: `Best Case`  •  Close Date: `2026-08-28`
- Primary products (SFDC): **Protect (TI Score (Device))**, **Protect (TI Score (Full))**, **Protect (TI Score (Identity))**
- Use case: Fraud mitigation in cash advance + LOC underwriting (separate fraud loss from credit loss)
- Qualified ACV: `$189,000`  •  Qualified MRR: `$18,900`  •  Revenue uplift est. `$378K` (63K auth calls / mo × $0.5 Ti Full × 12)
- Pain (verbatim): "They lack an affordable, scalable way to screen out fraud early in the funnel and can't reliably distinguish fraud losses from credit losses, which constrains how aggressively they can grow cash advance and LOC."
- Impact: "If Tilt can accurately identify and remove high‑risk users upfront, they can safely increase offer amounts and marketing spend while keeping overall loss rates in check."
- Decision process (SFDC, verbatim): "Use Plaid Protect (Trust Index) on PII and linked bank accounts to score user‑level fraud risk, separate first‑party from third‑party/account‑takeover fraud, and feed those insights into cash advance and LOC underwriting and step‑up flows."
- Stakeholders engaged (Gong + SFDC): Shirley (newly hired Fraud Program Manager — primary champion), Henry (credit / unit economics gatekeeper).
- Peer benchmark Plaid already shared with Tilt (Gong, Apr 15 2026): "stepping up roughly **5–10% of highest-risk Protect users captured about half** of related fraud losses" across other cash-advance customers — same pattern Tilt is targeting.
- Next step (SFDC, Apr 14 2026): "Greenlight from Tilt legal to proceed with Retro order form and kick off the Protect Retro process." This demo is the front-end of that Retro deck.
- AE: Matthew Stein  •  Manager: Xander O'Han  •  Account: Tilt Finance Inc. (~273 employees, Strategic Fintech)

**User journey (one sentence):**
A new Tilt customer named Maya Chen applies for a $400 cash advance, taps Connect bank account, completes one real Plaid Link session against her checking account, and Tilt's underwriter sees a Plaid Protect Trust Index "ACCEPT" with low return-risk scores + a clear ruleset result — clearing the application for instant funding while a hypothetical high-risk shadow journey would have been routed to REVIEW.

**Story arc (short prose):**
Maya needs $400 to bridge the gap before payday. Tilt's host flow walks her through a quick application (employer, monthly income, requested amount) and then asks her to connect her primary checking account so Tilt can verify income and screen for fraud. She taps Connect bank account. Inside Plaid Link she completes the standard flow against First Platypus Bank. Behind the scenes Tilt calls `/signal/evaluate` with her bank-account context, PII (name/email/phone), and device fingerprint. A short Plaid insight slide frames what Trust Index returns. Back in Tilt's underwriting console, the API panel shows the documented `/signal/evaluate` response — `scores.bank_initiated_return_risk` 12 (low), `scores.cash_advance` 18 (low), `core_attributes` showing stable inflows and a 196-day Plaid connection, `ruleset.result: ACCEPT` — and Tilt's host UI surfaces a "Trust Index — Accept" decision with a fraud-vs-credit breakdown. Maya gets her $400 instantly. Closing slide cites the peer benchmark (5–10% step-up catches ~50% of fraud) and Tilt's target outcome: separate fraud losses from credit losses, free budget for marketing without growing fraud.

-------------------------------------------------------------------------------
PRODUCTS & FAMILY
-------------------------------------------------------------------------------

**Products featured (approved names only):**
Plaid Link, **Plaid Protect** (umbrella), **Plaid Signal** (component — `/signal/evaluate`), **Plaid Trust Index** (Limited Availability — March 2026 cohort).

**Solutions supported** (Solutions Master):
Fraud Mitigation, Account Onboarding, Cash Advance / Earned Wage Access Underwriting.

**Primary product family** (pick one — aligns with pipeline profiles):
`plaid_protect`

**Primary messaging file for this demo:**
`inputs/products/plaid-protect.md` (canonical wire format, talk track, value props). Do not duplicate stats from `inputs/plaid-value-props.md`.

**Research depth:** gapfill

**Approved marketing stats (use at most one each in narration/slides):**
- Peer benchmark: **stepping up roughly 5–10% of highest-risk Protect users captures about half of related fraud losses** (Plaid AE Gong note Apr 15 2026; cited to Tilt internally — fine to reference in slides).
- Trust Index range **1–100, higher = SAFER user** (inverted direction vs Signal account-score; explicitly call this out in slide framing).
- Signal `bank_initiated_return_risk` range **1–99, higher = higher risk**.
- Sub-second decisioning at `/signal/evaluate` (documented in Plaid docs).

**Product status to disclose (truthful framing — HARD GUARDRAILS):**
- Plaid Protect umbrella + Trust Index = **Limited Availability** (Sales-gated). Mark this on slides.
- `/link/token/create` products: use **`['signal']`** (or `['signal', 'identity_verification']` if mentioning IDV). **NEVER** use `'protect'` — that's not a valid product string and will 400.
- Endpoint after Link onSuccess: **`POST /signal/evaluate`** with `client_transaction_id`, `access_token`, `account_id`, `amount`, `user{}`, `device{}`. **DO NOT FABRICATE** `/protect/event/send`, `/protect/user/insights/get`, `trust_index.*` API field shapes — those are NDA / private docs.
- `ruleset.result` values: **`ACCEPT`**, **`REROUTE`**, **`REVIEW`** only. **`REJECT` is NOT documented** — render any "decline" decision outside the API panel (host UI), never inside the JSON.
- Webhooks (literal names only): **`SIGNAL_SCORE_READY`**, **`SIGNAL_RULE_TRIGGERED`**, **`PROTECT_TX_MONITOR_RULE_TRIGGERED`**. Never paraphrase as `SCORE_READY` / `RULESET_TRIGGERED`.
- Explainability: use **`core_attributes`** (80+ documented signals) and **`ruleset.triggered_rule_details.internal_note`**. **NEVER fabricate a top-level `reason_codes[]` array** — that field does not exist.

-------------------------------------------------------------------------------
CONNECTION & RECORDING (checklist)
-------------------------------------------------------------------------------

  [x] **Live Plaid Link** — single `plaidPhase: "launch"` step; narrate inside modal only
  [x] **Sandbox:** First Platypus Bank (`ins_109508`) per `inputs/plaid-link-sandbox.md`; credentials `user_good` / `pass_good`; non-OAuth
  [x] **Link token products:** `['signal']` (add `'auth'` only if Tilt needs routing/account numbers — usually not required for fraud-only)
  [x] **Endpoint after Link onSuccess:** `POST /signal/evaluate` — show `scores.bank_initiated_return_risk` (12), `scores.cash_advance` (18), 2–3 `core_attributes`, `ruleset.result: ACCEPT`, `triggered_rule_details.internal_note`
  [x] **No `/user/create` bootstrap** — Signal/Protect uses standard Link flow

-------------------------------------------------------------------------------
STORYBOARD BEATS (ORDER = SCRIPT ORDER)
-------------------------------------------------------------------------------

| # | Beat | What the viewer sees | Narration focus | Reveal / CTA |
|---|------|----------------------|-----------------|--------------|
| 1 | slide | **Pure marketing slide** (`sceneType: "slide"`, NOT `"insight"` — no `apiResponse`). Plaid deck opener (T1 or T3): "Lend more. Lose less. Without giving fraud a head start." | Frame Tilt's challenge — they want to grow cash advance + LOC without losing the runway to fraud they can't separate from credit. | — |
| 2 | host | Tilt-branded onboarding: "Get up to $400 instantly. No credit check." with `Apply now` button. Light/white background, Tilt's orange/pink accent (from brand-extract). | Maya needs $400 to bridge the gap until payday. | `Apply now` |
| 3 | host | Tilt application form: employer name, monthly income, requested cash advance amount `$400`. Pre-Link explainer card below: "Connect your bank account so we can verify income and check for fraud." | Walks Maya through the cash advance application; ends narration on the user's tap that opens Plaid Link. | `Connect bank account` |
| 4 | link | Real Plaid Link launch (consent → institution search → First Platypus Bank credentials → checking-account select → success). `products: ["signal"]` Link token. | Narrate only what's visible inside the modal during the linking moment. | Link `onSuccess` |
| 5 | host | Tilt "Verifying your account…" loading state with a small fraud-screening progress indicator (white/light chrome). | Set expectation — Tilt is calling Plaid Protect behind the scenes. | — |
| 6 | slide | **Pure marketing slide** (`sceneType: "slide"`, NOT `"insight"` — no `apiResponse`). Plaid statement slide (T3, Plaid navy chrome): "One score. First-party, third-party, account-takeover." Mark **Trust Index — Limited Availability** on this slide. | What Plaid Protect Trust Index returns conceptually; the actual API JSON appears on host step 7, not on this slide. | — |
| 7 | host | Tilt **underwriting console** — operator-style screen with: decision card showing **"Trust Index — ACCEPT"** + low risk badge, fraud-vs-credit breakdown (e.g. "Fraud risk: low (12) · Credit risk: low (model 0.18)"), and 2–3 core attributes summarized as human-readable rows. **Collapsible `#api-response-panel`** shows the live `POST /signal/evaluate` response with `scores.bank_initiated_return_risk` 12, `scores.cash_advance` 18, 2–3 `core_attributes` (e.g. `available_balance: 2200`, `days_since_first_plaid_connection: 196`, `plaid_connections_count_7d: 0`), `ruleset.result: "ACCEPT"`, `triggered_rule_details.internal_note: "Stable inflows; low cross-network risk"`. | Hero reveal: Trust Index distinguishes fraud from credit risk; ACCEPT path means Maya gets funded instantly. Call out that the rule's internal note (not a `reason_codes` array) is the explainability surface. | `Approve advance` |
| 8 | host | Tilt approval success: "$400 on its way to your account" with instant-delivery option ($X fee) + repayment date pulled from Plaid income data. | Outcome — Maya is funded; Tilt's books now have a clean fraud-vs-credit signal for this loan. | `Done` |
| 9 | slide | **Pure marketing slide** (`sceneType: "slide"`, NOT `"insight"` — no `apiResponse`). Plaid stats slide (T4): peer benchmark callout — "**5–10% of highest-risk Protect users → ~50% of fraud losses**" with sub-line "Plaid customer benchmark, cross-cash-advance portfolio." | The lift Tilt is targeting; same pattern Plaid AE shared with Shirley on Apr 15. | — |
| 10 | slide | **Pure marketing slide** (`sceneType: "slide"`, NOT `"insight"` — no `apiResponse`). Closing CTA slide (T11): "Separate fraud from credit. Grow without growing losses." with sub-line referencing the Tilt Retro next step. | Close with the impact statement that maps to the SFDC critical event ("Greenlight from Tilt legal to proceed with Retro order form"). | — |

**Insight presentation rules:**
- API JSON on host step 7 uses the global `#api-response-panel` (collapsed by default per panel toggle contract; expanding reveals the `/signal/evaluate` response).
- **Slide steps (#1, #6, #9, #10) are pure marketing slides — `sceneType: "slide"` only. Do NOT use `sceneType: "insight"` on these; no `apiResponse` attached.** Insight (`sceneType: "insight"`) is reserved for Plaid-chrome screens that overlay actual API JSON on `.slide-root`, which this demo does not use — the JSON lives on host step 7's `#api-response-panel`.
- Do NOT show `'protect'` as a `products[]` value, `/protect/event/send`, `/protect/user/insights/get`, or any fabricated `trust_index.*` API field shape.
- Do NOT render `ruleset.result: "REJECT"` — use `REVIEW` / `REROUTE`, or render the decline outcome outside the API panel.
- Do NOT show a top-level `reason_codes[]` array — explainability lives in `core_attributes` + `ruleset.triggered_rule_details.internal_note`.
- Do NOT paraphrase webhook names. Only `SIGNAL_SCORE_READY` / `SIGNAL_RULE_TRIGGERED` / `PROTECT_TX_MONITOR_RULE_TRIGGERED`.

**Compliance / user data:**
Standard Link consent. No FCRA / CRA permissible-purpose layer (this is fraud screening, not consumer-report credit decisioning). No `/user/create` bootstrap. Cash advance host can show a generic privacy disclosure but should not reference Reg B / Reg E specific approvals on screen.

-------------------------------------------------------------------------------
PERSONA & SAMPLE DATA
-------------------------------------------------------------------------------

**Name / role:** Maya Chen — first-time Tilt cash advance applicant
**Company / context:** Tilt cash advance funnel; outside bank is sandbox First Platypus Bank checking account
**Sample fields that must appear on screen:**
- Requested amount `$400`
- `scores.bank_initiated_return_risk: 12` (low)
- `scores.cash_advance: 18` (low)
- 2–3 `core_attributes` rows, e.g. `available_balance: 2200`, `days_since_first_plaid_connection: 196`, `plaid_connections_count_7d: 0`
- `ruleset.result: "ACCEPT"`
- `ruleset.triggered_rule_details.internal_note: "Stable inflows; low cross-network risk"`
- Host UI shows "Trust Index — ACCEPT" with a low-risk badge
- Microcopy footnote on the underwriting console: "Powered by Plaid Protect — Trust Index (Limited Availability)"

-------------------------------------------------------------------------------
HOST CHROME (LIGHT TOUCH)
-------------------------------------------------------------------------------

Tilt consumer-facing app: top nav with Tilt wordmark, white/light backgrounds, brand-extracted accent colors (Tilt uses orange-to-pink gradients in their marketing). Application screens are single-column mobile-like cards centered on desktop with generous whitespace. The underwriting console (step 7) is presented as an internal-ops view — slightly denser layout, decision card + collapsible data panel on the right. Primary Link CTA label: **Connect bank account**. Approval success uses Tilt's "Cash is on the way" pattern.

**Viewport:** 1440×900.

-------------------------------------------------------------------------------
OPTIONAL: SLIDE SUPPLEMENT
-------------------------------------------------------------------------------

[[SLIDE_OUTPUT_BEGIN]]
Include **4 marketing slides** in `sceneType: "slide"` steps using Plaid Deck Design System (T1/T3 opener, T3 statement for Trust Index framing, T4 stats slide for the 5–10%/50% peer benchmark, T11 closing CTA). Use `data-slide-template` attributes T1/T3/T4/T11. Mark **Limited Availability** on any slide that names Trust Index. Keep `/signal/evaluate` JSON in the host `#api-response-panel`, not on slides.
[[SLIDE_OUTPUT_END]]

-------------------------------------------------------------------------------
PRE-FLIGHT
-------------------------------------------------------------------------------

  [x] Stats sourced from `inputs/products/plaid-protect.md` + Gong note (5–10% / ~50%) — not duplicated elsewhere
  [x] Product family `plaid_protect`; Link product `["signal"]`; endpoint `/signal/evaluate`
  [x] Live Plaid Link (single `plaidPhase: "launch"`); no simulated Link divs; First Platypus Bank sandbox
  [x] Slides enabled via SLIDE_OUTPUT block (app+slides build)
  [x] Trust Index = Limited Availability disclosure honest; no fabricated `/protect/*` endpoints; no `reason_codes[]`; no `REJECT`; webhook names literal

================================================================================
END OF SPECIFICATION
================================================================================